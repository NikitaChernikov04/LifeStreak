node_modules/
dist/
build/
# Every .env variant except the committed examples — .env.turso holds a live
# database token.
.env
.env.*
!.env.example
*.log
.DS_Store
.vscode/
.idea/

# Local SQLite database — never commit it (it holds real user rows once the
# app is live, and it would overwrite nothing useful for anyone else).
backend/prisma/*.db
backend/prisma/*.db-journal

*.tsbuildinfo

.vercel
.env*
# Re-allow the committed examples: the `.env*` line above (added by the Vercel
# CLI) would otherwise hide them from new clones.
!.env.example
!**/.env.example

.env*
