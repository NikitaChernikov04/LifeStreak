# Normalise to LF in the repository. The app is built and run on Linux
# (Docker, Fly, Vercel), so CRLF must never reach a container.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
*.db binary
